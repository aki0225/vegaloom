# GitHub 自动化规则

- Workflow 的权限保持最小化；新增写权限、Secret、OIDC、发布或部署步骤前必须说明用途和边界。
- 第三方 Action 固定到完整 Commit SHA。升级时核对官方发布说明，不使用浮动 Tag。
- CI 分片、触发路径和跳过条件属于验证合同。修改后要说明哪些 PR 会运行或跳过哪些检查，
  并覆盖受影响的路径判定。
- 发布流程必须绑定明确版本、Tag 和构建产物；不得从工作区脏状态或未经验证的提交发布。
- 日志、Artifact 和页面数据不得包含凭据、真实 Provider 输出、本机绝对路径或私有运行现场。
