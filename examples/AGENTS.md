# 示例与公开证据规则

- `briefs/` 和 `tasks/` 使用虚构、可独立理解的输入，不引用本机目录、私有仓库或真实凭据。
- `evidence/` 只保存已脱敏、可公开复核的证据。历史失败和 `needs_human` 结果不得改写成成功。
- 示例必须与当前公共 CLI 和产品合同一致；删除或更名命令时同步更新对应示例。
- 能由登记脚本生成的内容不要手工修改。生成器、来源和输出应在同一变更中保持一致。
