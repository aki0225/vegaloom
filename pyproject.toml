[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vegaloom"
version = "0.2.1"
description = "带监督执行、确定性验证和独立评审的 AI 编码 Harness"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8",
  "pyyaml>=6.0",
  "typer>=0.12",
]

[project.optional-dependencies]
dev = [
  "click>=8.1",
  "pytest>=8.0",
  "ruff>=0.5",
]

[project.scripts]
vega = "vega.cli_entrypoint:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
vega = ["resources/loops/*.yaml"]

[tool.ruff]
line-length = 100
target-version = "py311"
cache-dir = ".tmp/ruff/cache"

[tool.ruff.lint]
# 不依赖 Ruff 版本的隐式默认值，升级时必须显式评估新增规则。
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
cache_dir = ".tmp/pytest/cache"
