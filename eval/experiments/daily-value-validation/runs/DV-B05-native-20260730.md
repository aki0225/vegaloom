# DV-B05 Native 正式运行记录

- 日期：2026-07-30
- Case：`DV-B05`
- Treatment：`native`
- Run ID：`dv-b05-native-20260730`
- 结论：`infrastructure_failure`
- 最终处置：`not_completed`
- 隐藏重跑：无

## 1. 冻结合同

- baseline commit：`6ec99f89261b32f8a50848786eca055e1967659f`
- 模型：`gpt-5.6-sol`
- reasoning effort：`medium`
- Worker timeout：`1200` 秒
- 固定 verifier SHA-256：
  `2d5e44e6432bb34c436c24a3a8254f51da916efe5676adc1574cc4b2161e2584`
- Provider profile fingerprint：
  `f04e21fe35621dba4cddd010abdb9a8251b5200224df9f41b373fcdaef70ae17`

正式 workspace 由 baseline tree 重新导出，共 150 个 tracked 文件。workspace 只建立一个
中性本地基线提交，不含 remote 和上游 Git 历史。Worker 只接收冻结任务、当前源码、允许
路径和固定 verifier，不接收 Issue、PR、oracle 或另一 treatment 的产物。

正式环境使用 Python `3.14.3` 与 pytest `9.1.1`。这与资格确认使用的 Python `3.12.10`
不同，但调用前已在正式环境重新证明：

- baseline verifier 退出码为 `1`，四个 `color=False` 场景稳定为红；
- 目标 `tests/test_termui.py` 为 `229 passed, 11 skipped`。

该环境差异如需继续配对，另一 treatment 必须使用相同正式环境；本次 Native 未完成，
因此没有继续启动 Vega。

## 2. 启动前门禁

- workspace：clean、单提交、无 remote；
- baseline archive 与 workspace 的 150 个 tracked 文件内容一致；
- verifier hash 与冻结值一致；
- Provider profile 保留既有 Responses 路由，并关闭 hooks、Memory、Goal、浏览、插件和
  Multi-Agent；
- 严格线索扫描没有发现 Issue、PR、oracle SHA 或修复标题。

首次扫描曾因 `uv.lock` 的 SHA-256 字符串偶然包含裸数字 `3653` 而 fail-closed。Provider
尚未调用时已保留该阻断记录，并将规则收紧为带 Issue/PR 上下文的标识后复核为零命中。

## 3. 正式 Worker 结果

Native Worker 只启动一次，wall clock 为 `510.792` 秒：

```text
exit_code=1
timed_out=false
turn_completed=false
final_message_present=false
token_usage_present=false
```

Worker 已完成源码检查并写入 patch，随后 Provider 经本地代理连续返回 `502`、`503`。
Codex CLI 重连耗尽后以 `turn.failed` 结束，没有产生最终消息或 Token usage。公开记录不保存
Provider endpoint、代理端口或本机绝对路径。

失败现场的 tracked diff 严格位于允许路径：

```text
src/click/termui.py  | +3
tests/test_termui.py | +29
```

生产修改在 `_readline_prompt` 中根据 `resolve_color_default()` 尝试调用 `strip_ansi`，并新增
八组 `confirm / prompt × color × stdout/stderr` 测试。没有新增 tracked 或非忽略文件，
`git diff --check` 通过。

## 4. 封存验证

Worker 未正常返回后没有继续自动执行。封存阶段只读运行两个冻结命令，且验证前后 tracked
diff 与 verifier hash 均未变化：

| 验证 | 结果 |
|---|---|
| 固定 verifier | 退出码 `1`；四个 `color=False` 场景仍失败 |
| `tests/test_termui.py` | `4 failed, 233 passed, 11 skipped` |
| `git diff --check` | 退出码 `0` |
| allowlist | 通过 |

新增测试的四个 `color=False` 参数组合均观察到 ANSI 仍存在。当前证据只证明该部分 patch
没有满足冻结任务，不能因为 Provider 故障或其他 233 个测试通过而改写为成功。

## 5. Reviewer 与结论边界

- Reviewer：未启动。
- Reviewer Provider 请求：未发生。
- Reviewer 独立 finding：`0`。
- Token：input、output、cached input 均不可取得，记录为 `null`。
- Owner manual actions：`2`，对应正式调用结束后两次继续封存指令。
- recovery：未使用。
- 自动 commit、push、release：均未发生。

Worker 没有 `turn.completed`，且两项固定验证均失败。按 fail-closed 合同，Reviewer 的
approve 也不能覆盖这些失败，因此没有为获得 Reviewer 结果而继续调用 Provider。

本次结果登记为 `infrastructure_failure / not_completed`：Provider 中途故障使正式 Worker
无法完成，但失败现场的确定性验证也明确为红。该结果既不是 Native 成功，也不能单独形成
Native 能力失败或 Vega 日用价值结论。同一 treatment 不重跑，当前 pair 保持不完整。

## 6. 本地原始证据哈希

原始事件流和运行文件保存在忽略目录 `.local-validation/`，不提交其中的 Provider 诊断和
本机路径。用于本地复核的 SHA-256：

| Artifact | SHA-256 |
|---|---|
| `formal-worker-result.json` | `2645c74fc6c8dfb66dff66e7117dffb37d91e03c0408540d6d8065c752e2e41a` |
| `worker-events.jsonl` | `f85d9a86a609ed40a457965802e408e6980ffe1a62600249ca7bab12c62730a5` |
| `worker.diff` | `1ba777ded13ce6e065fd234f0290780635f99e5bec5b9b6a773e299f9998bc55` |
| `post-worker-verifier.stdout.json` | `2ebf304b8d461e16dcafc1ba0f51d776f91487421f9015bcd41c5eddf379e58d` |
| `post-worker-pytest.stdout.txt` | `86a966b8646f34539cadf02da3ad4bb38032207ebf347b33a8d55579adfdea79` |
| `seal-summary.json` | `73f624ff8c5bbb78ed6331ac265f1caa40efef5e3d2e3e973ffdbf03f27d7616` |
