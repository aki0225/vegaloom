# DV-B02 / Native 正式运行记录

- 日期：2026-07-29
- Case：`DV-B02`
- Treatment：`native`
- Run：`dv-b02-native-20260729`
- 结论：`timed_out`
- 最终处置：`not_completed`
- 隐藏重跑：无

## 1. 冻结合同

- baseline commit：`ee0f19b696c60064c58cdc08b3265aef56d49ff8`
- 模型：`gpt-5.6-sol`
- reasoning effort：`medium`
- Worker timeout：`600` 秒
- 固定 verifier SHA-256：
  `36111049980d30ad126ddbd1fc0247c870f068eeb3db01def559312f099b1b47`
- Provider profile fingerprint：
  `f04e21fe35621dba4cddd010abdb9a8251b5200224df9f41b373fcdaef70ae17`

正式 workspace 由 `git archive` 导出，只建立一个中性本地 baseline commit，不含 remote
和上游 Git 历史。Native Worker 只接收冻结任务合同、允许路径、固定 verifier 和通用执行
约束，不接收 Vega treatment 的任何产物。

## 2. 启动前门禁

- baseline verifier：退出码 `1`，符合冻结红态。
- baseline 规范化 stdout SHA-256：
  `105f943da8f39426f1f244ea4fe0516dd398a1264526f02bb5f641c6aa99ef57`
- Worker 与 Reviewer Provider profile：通过离线预检，fingerprint 一致。
- baseline 与 workspace：各 `123` 个文件。
- workspace：clean、单提交、无 remote。
- 冻结答案线索扫描：无命中。

## 3. 正式运行结果

Native Worker 在 `600` 秒期限内没有返回最终消息，进程树终止已确认：

```text
run_status=timed_out
exit_code=1
reviewer_status=not_run
```

Worker wall clock 为 `608.106` 秒。事件流没有完整 Token usage，因此 input、output 和
cached input Token 均记为 `null`。

## 4. 超时现场

超时时保留了一个部分完成的 patch：

- 修改 `src/attr/converters.py` 与 `tests/test_converters.py`；
- diff 为 `+85 / -5`；
- 没有新增文件；
- `git diff --check` 通过；
- 修改路径没有越过冻结 allowlist。

Worker 已导入并包装 `Converter`，同时补充 `None` 短路、转换顺序和上下文传播测试。其
目标 pytest 命令因当前正式环境没有安装 `hypothesis` 而失败；Worker 随后启动不依赖该
测试依赖的源码 smoke，但在命令返回前达到 timeout。

Native 编排不会在 Worker 超时后自动运行验证。封存阶段只读执行同一固定 verifier，退出码
为 `0`，四项冻结语义全部通过；verifier 执行前后 SHA-256 保持不变。

## 5. Reviewer 与结论边界

- Reviewer：未启动。
- Reviewer 独立 finding：`0`。
- recovery：未使用。
- 自动提交、push、release：均未发生。

固定 verifier 通过只说明超时现场的 patch 满足预注册行为，不会把未返回、未完成独立
Reviewer 的 treatment 改写为成功。本结果仍登记为 `timed_out / not_completed`。
