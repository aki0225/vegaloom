# DV-B05 Native 重跑 V3 正式运行记录

- 日期：2026-07-30
- Case：`DV-B05`
- Case revision：`5`
- 实验版本：`direct-rerun-v3`
- Treatment：`native`
- Run ID：`dv-b05-native-r3-20260730`
- 运行状态：`completed`
- 最终处置：`needs_human`
- 比较资格：`pending_human_adjudication`
- 隐藏重跑：无

## 1. V3 边界

前两次 Native 结果继续保留：

- `dv-b05-native-20260730`：Provider 中途 `502/503`，无 `turn.completed`，验证为红；
- `dv-b05-native-r2-20260730`：Provider stream 断开后超时，无修改，验证为红。

Owner 再次确认 Provider 已恢复后，V3 使用全新的 baseline-only workspace、venv、临时目录、
Codex 会话和证据目录。Worker 不接收前两次运行的 patch、事件流、验证结果或结论。本版本
只执行一次正式 Worker；只有 Worker 正常完成且两个固定验证全绿，才启动独立只读
Reviewer。

## 2. 调用前门禁

首次本地门禁发现 V3 venv 未通过 `PYTHONPATH` 指向 workspace 的 `src/`，目标 pytest 因
`ModuleNotFoundError: click` 退出 `4`。该阻断发生在任何 Provider 请求之前，原始 blocked
记录被保留；随后使用与源码布局一致的 `PYTHONPATH=workspace/src` 重新执行门禁。

正式启动时：

- baseline archive 与 workspace 的 150 个 tracked 文件内容一致；
- workspace clean、单提交、无 remote；
- Issue、PR、oracle 和旧运行线索扫描为零命中；
- verifier SHA-256 为
  `2d5e44e6432bb34c436c24a3a8254f51da916efe5676adc1574cc4b2161e2584`；
- baseline verifier 退出码为 `1`；
- baseline `tests/test_termui.py` 为 `229 passed, 11 skipped`；
- Provider profile fingerprint 与冻结值一致；
- Provider 请求尚未发生。

## 3. 正式 Worker

Worker 正常产生一个 `turn.completed`，wall clock 为 `706.705` 秒：

```text
exit_code=0
timed_out=false
termination_confirmed=true
input_tokens=548595
cached_input_tokens=502144
output_tokens=5525
```

修改严格限于允许路径：

```text
src/click/termui.py  | +7
tests/test_termui.py | +29
```

生产代码在 `_readline_prompt` 中根据实际输出流复用 Click 的颜色判定和 ANSI 清理语义；测试
覆盖 `confirm / prompt × color × err` 八种组合。

Worker 在 sandbox 内执行固定 pytest 时，因 `../pytest-temp` 位于 workspace 外且不可写而
得到四个 `PermissionError`。这是 sandbox 与冻结 basetemp 路径的合同冲突，不是测试断言
失败，也不是 Provider 故障；因此随后由封存阶段在同一 workspace 上独立执行固定验证。

## 4. 封存验证

| 验证 | 结果 |
|---|---|
| 固定 verifier | 退出码 `0`；8 个场景全部通过 |
| `tests/test_termui.py` | `237 passed, 11 skipped` |
| `git diff --check` | 退出码 `0` |
| allowlist | 仅两个允许文件 |

Worker diff 在 Reviewer 前后保持一致。封存验证证明当前 patch 满足冻结行为，但不能单独覆盖
Reviewer verdict。

## 5. 独立 Reviewer

Reviewer 使用新的只读 Codex 会话，不接收 Worker 聊天、事件流、自述、最终消息、旧运行或
另一 treatment。Reviewer 正常产生一个 `turn.completed`，wall clock 为 `391.512` 秒，
workspace 未改变：

```text
exit_code=0
timed_out=false
verdict=needs_human
reported_findings=1
```

Reviewer 没有发现代码或测试缺陷。唯一 finding 是输入证据没有显式包含 verifier 在固定
验证前后的实际 SHA-256 记录，因此按“证据不足必须 needs_human”的合同拒绝 approve。

Reviewer 完成后补充执行了一次带前后哈希的固定验证：

- verifier 前后 SHA-256 均为冻结值；
- verifier 再次退出 `0`；
- pytest 再次得到 `237 passed, 11 skipped`。

该补充证据不追溯改写首次独立 Reviewer verdict，也没有再次调用 Reviewer。

## 6. 结论边界

- 本次 Worker 与 Reviewer 的 Provider 调用都正常完成，前两轮 Provider 故障没有复现。
- 当前实现通过确定性验证，Reviewer 也没有报告代码缺陷。
- 自动成功条件仍未满足，因为首次独立 Reviewer verdict 是 `needs_human`。
- Reviewer 报告 finding 数为 `1`；经确认的独立代码缺陷数仍为 `0`。
- Owner manual actions：`0`，正式 Worker 启动后没有人工搬运、重启或恢复。
- recovery：未使用。
- Vega treatment：尚未授权，也未启动。

本次结果登记为 `completed / needs_human / pending_human_adjudication`。它证明 Provider
链路和独立 Reviewer 机制这次真实跑通，但在人工确认 Reviewer 的证据 finding 前，不能记为
`verified_success`，也不能继续形成 Native/Vega 价值比较。

## 7. 本地原始证据哈希

原始文件保存在忽略目录 `.local-validation/`。公开记录不保存 Provider endpoint、代理端口
或本机绝对路径。

| Artifact | SHA-256 |
|---|---|
| `formal-worker-result.json` | `dda8ca19c50fa7c5e133a8a1aa5444d435a0a740b137b3498815ca5c551fee63` |
| `worker-events.jsonl` | `0f9e3cf764a0150d80da90205473a38c14041d85d5ad2b5f1edd931a0c5ca8b2` |
| `worker.diff` | `c499e839155090d9ad3b666db7a8a305d5b2f5078c50963e0a4561ca43082b78` |
| `post-worker-verifier.stdout.json` | `1bb4dc9e8f018262d80a2108e63eb738f7038cb07f1621ed6fc99c3bdbe0132c` |
| `post-worker-pytest.stdout.txt` | `2ac97bf95976481fc0d2a7494179c6658f4d2baed289c329a4e0fdfbaec04517` |
| `formal-reviewer-result.json` | `10ddbce4fc246ff564f3357b7cef064fca84c0a4362e3490886adf3f63681345` |
| `reviewer-events.jsonl` | `c58b7cbd7f4ae14b4c916ca25d111371082cbad47a32e04ca8c0ed05fc89c247` |
| `reviewer-output.json` | `44e51040b5fca43d4999dfacfd4af11fc202bb90d0fdb8a140edde3558cac2d8` |
| `verifier-integrity-evidence.json` | `1437a34276206c6479227f4b3dccf64f604a8dbc04b8aedd1de48bbd6c12e695` |
| `seal-summary.json` | `32dd3cd5c3b2f5891f766d6f93cba2580c54c042e44efc8c590ffbc8db96b8e0` |
