# DV-B02 / Vega 正式运行记录

- 日期：2026-07-29
- Case：`DV-B02`
- Treatment：`vega`
- Runtime run：`20260729-160544-195187-bug-loop`
- 结论：`timed_out`
- 最终处置：`not_completed`
- 隐藏重跑：无

## 1. 冻结合同

- baseline commit：`ee0f19b696c60064c58cdc08b3265aef56d49ff8`
- 模型：`gpt-5.6-sol`
- reasoning effort：`medium`
- Worker timeout：`600` 秒
- 最大自动迭代：`1`
- 固定 verifier SHA-256：
  `36111049980d30ad126ddbd1fc0247c870f068eeb3db01def559312f099b1b47`
- Provider profile fingerprint：
  `f04e21fe35621dba4cddd010abdb9a8251b5200224df9f41b373fcdaef70ae17`

正式 workspace 由 `git archive` 导出，只建立一个中性本地 baseline commit，不含 remote
和上游 Git 历史。`.vega.yaml` 固定允许路径、验证命令和 Worker/Reviewer Codex profile。

## 2. 启动前门禁

- baseline verifier：退出码 `1`，符合冻结红态。
- baseline 规范化 stdout SHA-256：
  `105f943da8f39426f1f244ea4fe0516dd398a1264526f02bb5f641c6aa99ef57`
- `vega config check`：通过。
- Worker 与 Reviewer Provider profile：通过离线预检，fingerprint 一致。
- workspace：clean、单提交、无 remote。
- 冻结答案线索扫描：无命中。

首次门禁曾在 Provider 调用前停止，因为操作脚本把 Windows CRLF 落盘文件的原始字节 hash
与资格阶段的 LF 规范化 stdout hash 直接比较。复核后确认规范化 hash 完全一致；该次停止
没有启动 Worker、Reviewer 或 Provider 请求，因此不构成 treatment 重跑。

## 3. 正式运行结果

Worker 在 `600` 秒期限内没有返回，Vega 将其标记为 `timed_out`，确认子进程已终止，并按
fail-closed 语义停止后续 verification 和 Reviewer。Runtime 最终状态为：

```text
status=needs_human
current_step=timed_out
worker_status=timed_out
reviewer_status=skipped
```

端到端 wall clock 为 `627.369` 秒。Worker 输出没有完整 Token usage，因此 input、output
和 cached input Token 均记为 `null`。

## 4. 超时现场

超时时保留了一个未完成 patch：

- 只修改 `src/attr/converters.py`；
- diff 为 `+22 / -4`；
- 没有新增文件；
- 没有修改 `tests/test_converters.py`；
- `git diff --check` 通过；
- 修改路径没有越过冻结 allowlist。

该 patch 引用了未导入的 `Converter`。Vega 因 Worker 超时没有运行固定验证；封存阶段只读
执行同一 verifier，退出码为 `1`，四项检查都因 `NameError: name 'Converter' is not
defined` 失败。verifier 执行前后 SHA-256 保持不变。

## 5. Reviewer 与结论边界

- Reviewer：未启动。
- Reviewer 独立 finding：`0`。
- recovery：未使用。
- 自动提交、push、release：均未发生。
- Native treatment：本记录封存前未启动。

本结果是一次可比较的超时失败，不是基础设施故障，也不是验证成功。它只能证明当前
Vega treatment 在冻结的 600 秒合同内没有完成任务；不能据此单独判断 Vega 整体是否有
日用价值。
