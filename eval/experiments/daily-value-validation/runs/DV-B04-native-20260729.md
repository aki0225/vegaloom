# DV-B04 Native 正式运行记录

- 日期：2026-07-29
- Case：`DV-B04`
- Treatment：`native`
- Run ID：`dv-b04-native-20260729`
- 结论：`infrastructure_failure`
- 能力结论：不可形成
- 隐藏重跑：无

## 1. 冻结合同

- baseline：`8c95c73bd5ef89eac638f85f1904a104ba4b1a32`
- 模型：`gpt-5.6-sol`
- reasoning effort：`medium`
- timeout：`600` 秒
- verifier SHA-256：
  `3d549fc0f033cd6fe126946eb63f59536439ed9fac0a6c1c7384cd6cc4367bf0`
- Worker prompt SHA-256：
  `3157d5925b273a947e040384a32cd61e48b467e9825978bf39bf4438f0c98ba8`
- Reviewer contract SHA-256：
  `107e6eef6b73cd32b995851ecc9f18c52e950889560b419f8ffdea843086e75e`

正式 workspace 由 baseline tree 重新导出，共 149 个文件，不包含 `.git`。调用前固定
verifier 退出码为 `1`，且 verifier 运行没有改变 workspace。

## 2. 运行结果

Codex CLI 在 147.307 秒后以退出码 `1` 结束，没有超时。事件流包含一次
`thread.started`、一次 `turn.started`、十条连接错误和一次 `turn.failed`，没有
`turn.completed`、Token 统计或最终消息。

失败发生在模型输出前：为了隔离本机 hooks、Memory、Goal、多 Agent 和浏览能力，启动命令
使用了 `--ignore-user-config`；该选项同时移除了本机自定义 Provider 路由，认证请求因而发往
不匹配的默认路由并返回 `401`。

## 3. 文件与验证

- 变更文件：`0`
- 越界文件：`0`
- Worker 后 verifier 退出码：`1`
- verifier hash：未变化
- Reviewer：未启动
- Token：不可用
- 人工恢复：无

Worker 后 verifier 输出 SHA-256 为：

```text
38948737ebc494e7c32ebcf1497ebfb510ef056b07b17926815ee864cf94ca97
```

它与资格确认阶段 baseline 的代表性红态输出 hash 一致。

## 4. 原始证据哈希

原始运行文件保留在忽略目录 `.local-validation/`，不提交包含本机路径和脱敏凭据片段的
原始事件流。用于本地复核的 SHA-256：

| Artifact | SHA-256 |
|---|---|
| `worker-events.jsonl` | `73115b223e4e052734ea3f30a5cef3dbf8901bb0b17a1603e3fdce3bc19231db` |
| `worker-process-result.json` | `751cbd491ac0552e692a90c9fd5d9fded6bb67543dc62b33e1d49469414bb40d` |
| `worker-change-manifest.json` | `3e70fdd9287a606424b1d5a5eb55f4fe3637b762f5e41583ba2ac0e997f4047b` |
| `worker-verifier.stdout.json` | `38948737ebc494e7c32ebcf1497ebfb510ef056b07b17926815ee864cf94ca97` |
| `run-manifest.json` | `e7e3c5cb59bf09d89637e7dbdfe041aa1392c8766cd5fbb2902bb227226ce25b` |

## 5. 裁决

这是实验启动边界错误造成的基础设施失败，不是 Native Worker 的能力失败。根据 V1 合同，
同一 treatment 不进行隐藏重跑，DV-B04 pair 保持不完整，也不启动 Vega treatment。

下一次新的正式 treatment 前，必须先离线验证隔离配置既关闭非实验变量，又保留冻结的
Provider 路由。该修正属于实验启动器，不构成修改 Vega Runtime 的理由。
