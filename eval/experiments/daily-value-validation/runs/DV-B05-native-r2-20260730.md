# DV-B05 Native 重跑 V2 正式运行记录

- 日期：2026-07-30
- Case：`DV-B05`
- Case revision：`4`
- 实验版本：`direct-rerun-v2`
- Treatment：`native`
- Run ID：`dv-b05-native-r2-20260730`
- 结论：`timed_out`
- 最终处置：`not_completed`
- 比较资格：`invalid_infrastructure`
- 隐藏重跑：无

## 1. 重跑边界

上一轮 `dv-b05-native-20260730` 继续作为 Provider 中途故障的无效样本保留。Owner 明确授权
后，本版本在正式调用前完成独立预注册：

- 使用新的 baseline-only workspace、单提交、无 remote；
- 不读取上一轮 patch、事件流、验证结果或结论；
- 使用新的 Codex 会话、临时目录、venv 和运行证据目录；
- Native Worker 在本版本只允许一次正式调用；
- 只有 Worker 正常 `turn.completed` 且两个固定验证全绿，才允许启动 Reviewer。

正式调用前发现当前 Provider profile 名称发生变化，但脱敏 origin、Responses 协议、模型
目录、执行 profile 和隔离设置保持一致。该变化在任何 Provider 请求前以 revision `4`
冻结，Worker 与 Reviewer 要求匹配的新 profile fingerprint 为：

```text
4a7e838f4d6e027eb240a4d3fe3a0db53ac1caa430ff7cd5106a3b81c9d07cb0
```

## 2. 调用前门禁

- baseline archive 与 workspace 的 150 个 tracked 文件内容一致；
- workspace clean、单提交、无 remote；
- Issue、PR、oracle 和旧运行线索扫描为零命中；
- verifier SHA-256：
  `2d5e44e6432bb34c436c24a3a8254f51da916efe5676adc1574cc4b2161e2584`；
- baseline verifier：退出码 `1`，四个 `color=False` 场景为红；
- baseline `tests/test_termui.py`：`229 passed, 11 skipped`；
- Python：`3.14.3`；
- pytest：`9.1.1`。

Owner 已说明 Provider 当前恢复稳定，因此没有增加额外模型健康请求。

## 3. 正式 Worker 结果

Worker wall clock 为 `1204.434` 秒，达到冻结的 1200 秒 timeout 后终止进程树并确认退出：

```text
exit_code=1
timed_out=true
termination_confirmed=true
turn_completed=false
token_usage_present=false
```

事件流显示 Worker 只完成初始源码检索和 verifier hash 核对。随后 Provider stream 在完成前
断开并报告 transport decode error，此后没有恢复到可继续执行的状态。直到 timeout：

- 没有生产代码或测试修改；
- 没有 tracked diff；
- 没有非忽略 untracked 文件；
- 没有最终消息；
- 没有 Token usage。

这说明离线 profile 门禁和 Owner 的稳定性确认不能代替正式调用的 live Provider 行为。

## 4. 封存验证

超时后只读执行固定验证：

| 验证 | 结果 |
|---|---|
| 固定 verifier | 退出码 `1`；四个 `color=False` 场景仍失败 |
| `tests/test_termui.py` | `229 passed, 11 skipped` |
| `git diff --check` | 退出码 `0` |
| tracked diff | 空 |

结果与 baseline 一致，证明 Worker 没有形成修复。

## 5. Reviewer 与结论边界

- Reviewer：未启动。
- Reviewer Provider 请求：未发生。
- Reviewer 独立 finding：`0`。
- Token：input、output、cached input 均为 `null`。
- Owner manual actions：`1`，对应超时后的继续封存指令。
- recovery：未使用。
- 自动 commit、push、release：均未发生。

Worker 超时、没有 `turn.completed`、没有 patch，且固定 verifier 为红，因此不满足 Reviewer
启动条件。本版本不再次重跑，也不继续 Vega treatment。

该结果登记为 `timed_out / not_completed / invalid_infrastructure`。它不能形成 Native
能力成功、能力失败或 Vega 日用价值结论；只能证明当前 live Provider 链路仍不足以支撑这次
20 分钟正式实验。

## 6. 本地原始证据哈希

原始文件保存在忽略目录 `.local-validation/`。公开记录不保存 Provider endpoint、代理端口
或本机绝对路径。

| Artifact | SHA-256 |
|---|---|
| `formal-worker-result.json` | `0dfb2491199a43eef6f1b49106f770876fca7818822c9ece8adf59a613bd84b5` |
| `worker-events.jsonl` | `21205e9b20f2d7eb4343e35c0e1ec909667e2d198b940ba942e690f7a2ff6300` |
| `worker.diff` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `post-worker-verifier.stdout.json` | `2ebf304b8d461e16dcafc1ba0f51d776f91487421f9015bcd41c5eddf379e58d` |
| `post-worker-pytest.stdout.txt` | `f331ec24cff59ec1488f93d0324129529495c6ec59ed9f7172db1052c7a35f84` |
| `seal-summary.json` | `d58f48066ba94d2f227da0d7fe1f05a6f65fedb6e64da2b494460e08ce7c0c1f` |
