# 日用价值实验 V2 接力说明

- 日期：2026-07-29
- 分支：`experiment/ma2b-pilot-next`
- 实现提交：`9adba80`
- 当前阶段：V1 证据已封存，V2 基础设施已实现，尚未启动 V2 正式 treatment

## 已完成

1. 提交 DV-B01、DV-B02、DV-B04 的资格记录，以及 DV-B02 配对运行和 DV-B04
   基础设施失败记录。
2. 新增 V1 case/result 校验与 Codex profile 离线预检。
3. 新增 V2 共用环境资格门：
   - Python 与安装包 `environment_fingerprint`；
   - `pip check`；
   - 目标 pytest `--collect-only`；
   - Python 启动和 Git 状态控制延迟；
   - 正式 treatment 并发与竞争负载声明。
4. 新增 V2 Worker 驱动：
   - 逐行记录 Codex JSONL 的 UTC `received_at`；
   - 拒绝危险参数和既有正式产物；
   - 超时只终止自身创建的进程树。
5. V2 结果字段已经区分：
   - `runtime_verification_status`；
   - `post_seal_verification_status`；
   - `owner_manual_actions`；
   - `automation_actions`。

V1 历史记录没有改写，Vega 核心 Runtime 没有为实验迁就修改。

## 已完成验证

```text
python -m pytest ... tests/test_daily_value_eval.py tests/test_daily_value_v2.py
36 passed in 0.28s

python -m compileall ...
通过

ruff check ...
通过

python -m pytest --collect-only -q
815 tests collected in 2.99s

python scripts/check_repository_hygiene.py
通过

git diff --check
通过
```

## 未完成验证

- `python scripts/check_repository_hygiene.py --base-ref origin/main` 在当前同机高负载下未获得
  完整终态；第一次发现测试 fixture 缺少豁免标记，已修复，第二次运行超时后被中断。
- 没有再次裸跑全量 pytest。此前有界诊断确认收集正常，执行停在风险证据重算的 Git
  子进程；同机另有长 pytest 和多个 Codex 会话，不能把该现象归因给本次 V2 代码。
- 没有运行 V2 正式 pair，也没有调用 Provider 验证 V2 Worker 驱动。

因此当前可以声称“V2 实验门禁和记录能力已实现并通过定向自动化测试”，不能声称“V2
实验已成功”或“Vega 已证明日用价值”。

## 下一步

优先在负载较低的环境完成门禁，不继续增加 Harness 功能：

```powershell
git fetch origin
git switch experiment/ma2b-pilot-next
git pull --ff-only

python scripts/check_repository_hygiene.py --base-ref origin/main
python scripts/check_architecture_growth.py --base-ref origin/main
python -m pytest
```

门禁通过后，只选择一个新的 V2 case：

1. 建立 Native 与 Vega 共用的测试 venv，并安装完整测试依赖。
2. 固定 case 的测试收集目标和控制延迟上限。
3. 为两组分别执行 `daily_value_v2.py preflight`。
4. 确认两组均为 `ready` 且 `environment_fingerprint` 完全相同。
5. 再决定是否各执行一次正式 Worker 和独立 Reviewer。

详细命令和结果 schema 见
`docs/experiments/daily-value-validation/PAIRED-DOGFOOD-V2.md`。

## 继续时不要做

- 不重跑或改写 DV-B02、DV-B04 的 V1 正式结果；
- 不修改 Vega Runtime 来迁就候选仓库；
- 不扩建 SDK、Web UI、Memory、Multi-Worker 或 A2A；
- 不在 preflight 为 `blocked` 时调用 Provider；
- 不把 post-seal verifier 的绿态改写成已完成 Worker。
