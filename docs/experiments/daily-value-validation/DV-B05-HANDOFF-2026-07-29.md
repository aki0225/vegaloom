# DV-B05 日用价值直接配对交接

> 2026-07-30 最新状态：重跑 V3 的 Worker、固定验证和独立只读 Reviewer 均已真实完成。
> 实现与验证为绿，Reviewer 没有发现代码缺陷，但因输入证据漏交 verifier 前后哈希而返回
> `needs_human`。补充哈希证据已全绿，但未追溯改写 verdict，也未重跑 Reviewer。正式证据
> 见 `eval/experiments/daily-value-validation/runs/DV-B05-native-r3-20260730.md`。
>
> 2026-07-30 最新状态：Owner 将首个 Native 结果判定为不可比较，并预注册重跑 V2。
> V2 Worker 在初始检索后发生 Provider stream transport error，1200 秒超时且 workspace
> 无修改；Reviewer 未启动，Vega treatment 不继续。正式证据见
> `eval/experiments/daily-value-validation/runs/DV-B05-native-r2-20260730.md`。
>
> 2026-07-30 状态更新：本文保留 2026-07-29 的调用前交接背景，但原授权停点已经失效。
> Native treatment 已正式运行一次并登记为
> `infrastructure_failure / not_completed`；固定验证为红，Reviewer 未启动，Vega
> treatment 不继续。正式证据见
> `eval/experiments/daily-value-validation/runs/DV-B05-native-20260730.md`。
>
> 交接日期：2026-07-29
>
> 继续分支：`experiment/ma2b-pilot-next`
>
> 当前阶段：`rerun_v3_sealed / needs_human / vega_not_authorized`
>
> 当前权威计划：
> `docs/experiments/multi-agent-coordination/RESEARCH-AND-EXPERIMENT-PLAN.md`
> 第 12.16 至 12.20 节

## 一、这个分支现在负责什么

本分支最初用于 MA-2B Multi-Worker 能力实验。当前有效结论已经冻结为：

```text
Multi-Worker 机械能力成立
经济收益未观察到
不进入产品化
```

Owner 已停止继续扩建 MA Harness、A2A、认证门、事件采集器或新的证据层。当前分支的唯一
活跃任务已经转为日用价值直接配对：

> 对同一个 10 至 20 分钟真实 Bug，比较 Native Codex 与 Vega 工作流，在确定性质量不下降
> 的前提下，Vega 是否减少人工操作，并提供有实际用途的独立审查和交付追溯。

因此，下一步不是继续补 C01-C12 正式 Pilot readiness，也不是恢复已删除的 V2 Harness，
而是先裁决 `DV-B05/native` V3 的证据 finding，再决定是否单独授权 Vega treatment。

## 二、本轮已经完成什么

提交 `e9d0900` 已完成 `DV-B05` 输入资格冻结：

1. 将 `DV-B03` 追加为 `revision=2, status=retired`：
   - 任务预计只需单行修复；
   - Issue 评论直接给出修法；
   - 不满足 10 至 20 分钟代表性和盲测边界；
   - 未调用 Provider，也未继续构造 verifier。
2. 将 `DV-B05` 追加 candidate 与 runnable revision。
3. 冻结唯一 Worker 任务合同：
   `docs/experiments/daily-value-validation/tasks/DV-B05.md`。
4. 固定独立 verifier：
   `eval/experiments/daily-value-validation/verifiers/DV-B05.py`。
5. 提交完整资格记录：
   `eval/experiments/daily-value-validation/qualifications/DV-B05.md`。
6. 更新 V1 supersession 和总研究计划，明确：
   - 不恢复 runner、ledger schema、事件采集器或专用测试 Harness；
   - `worker_token_limit` 只是观测预算，不是硬门禁；
   - Provider、Worker 与 Reviewer 均未调用。

资格冻结后已依次保留首次 Native、重跑 V2 和重跑 V3 的独立结果。只有 V3 的 Worker、
确定性验证和 Reviewer 全部完成；其最终处置仍为 `needs_human`。这些运行都没有修改 Vega
Runtime、CI、MA-3、A2A 或 Multi-Worker 实现。

## 三、冻结的 DV-B05 合同

上游任务：`pallets/click #3572`，禁用颜色时 `confirm` 和 `prompt` 的交互提示仍保留
ANSI 样式代码。

```text
baseline: 6ec99f89261b32f8a50848786eca055e1967659f
oracle:   fe3ad76e5807e7a401ed7520f051071c6ae1fa6e
order:    Native -> Vega
model:    gpt-5.6-sol
reasoning: medium
timeout:  1200 seconds
```

允许修改：

```text
src/click/termui.py
tests/test_termui.py
```

固定验证：

```powershell
python ../verifier.py .
python -m pytest -q tests/test_termui.py --basetemp ../pytest-temp
```

verifier SHA-256：

```text
2d5e44e6432bb34c436c24a3a8254f51da916efe5676adc1574cc4b2161e2584
```

每个 treatment 只允许一次正式运行。第二组不得读取第一组 patch、运行结论或 Reviewer
finding。

## 四、资格验证结果

Windows 10 / Python 3.12.10：

| Ref | verifier 三次退出码 | verifier 输出 SHA-256 | 目标 pytest |
|---|---|---|---|
| baseline | `1 / 1 / 1` | `f96c3298e99e819b1f45974acb6e88cf6cedc9ae1c1e052f44f94cb5c4186c1c` | `229 passed, 11 skipped` |
| oracle | `0 / 0 / 0` | `925f672e0412f5f7c6b1c02fc9be719fa40aa9e49fb1b7fabb29d28a4d8d3b55` | `233 passed, 11 skipped` |

两个 ref 均从源码安装 `click 8.5.0.dev0`、通过 `pip check`。baseline-only archive：

- 共 150 个文件；
- 不包含 `.git`；
- 不包含 Issue、PR、oracle 或修复标题精确线索；
- verifier 运行前后文件清单与内容 hash 不变。

仓库级验证：

- `python -m compileall ...`：通过；
- Ruff：通过；
- 仓库路径与私密文件卫生：通过；
- 本轮相对 `HEAD` 的架构增长门禁：通过，`C901 46->46`、Python 模块 `63->63`；
- 仓库卫生与架构增长相关测试：`52 passed`；
- `git diff --check`：通过。

裸跑全量 pytest 和四个大分片都在单次 60 秒上限内未完成，不能记为全量通过；没有观察到
断言失败。本轮没有为了追求全量绿态重新增加测试 Harness。

## 五、当前还缺什么

1. Owner 尚未对 V3 Reviewer 的证据 finding 做人工裁决。
2. V3 不能记为 `verified_success`，当前最终处置保持 `needs_human`。
3. 尚未授权或创建 Vega treatment。
4. 尚未形成 Native/Vega 完整 pair。
5. 尚未形成 Vega 日用价值或经济性结论。

当前已证明 Native Worker、独立 Reviewer 和确定性封存链路可以真实完成；尚未证明 Vega
相对 Native 有日用价值。

## 六、明天从其他电脑继续

已有本地分支：

```powershell
git fetch origin --prune
git switch experiment/ma2b-pilot-next
git pull --ff-only origin experiment/ma2b-pilot-next
git status --short --branch
git log -5 --oneline --decorate
```

本地没有该分支：

```powershell
git fetch origin --prune
git switch --track -c experiment/ma2b-pilot-next origin/experiment/ma2b-pilot-next
git status --short --branch
git log -5 --oneline --decorate
```

继续前只需阅读：

1. 本文；
2. `eval/experiments/daily-value-validation/runs/DV-B05-native-r3-20260730.md`；
3. `docs/experiments/daily-value-validation/DV-B05-RERUN-V3.md`；
4. `docs/experiments/multi-agent-coordination/RESEARCH-AND-EXPERIMENT-PLAN.md`
   第 12.20 节。

随后停在授权点：

```text
是否接受 V3 的补充完整性证据，并单独授权创建 DV-B05/Vega treatment？
```

未获得明确裁决前，不把 Native V3 改写为成功，也不启动 Vega。

## 七、持续有效的停止线

- 不修改或重跑 DV-B02、DV-B04 的历史 V1 正式结果；
- 不恢复 `daily_value_eval.py`、V2 runner、preflight 或事件采集器；
- 不修改 Vega 通用 Runtime 来迁就 DV-B05；
- 不新增 SDK、Web UI、Memory、Goal、A2A、Planner 或 Multi-Worker；
- 不读取、复制或提交真实 API key、认证文件或本机绝对路径；
- 不复用资格确认 workspace 作为正式 treatment；
- 不把 verifier 绿态改写成 Worker 已完成；
- 不把 timeout 描述成通过；
- 不在未授权时调用 Provider、Worker 或 Reviewer；
- 不合并到 `main`，不创建新的 MA 实验分支。

## 八、远端 CI

`.github/workflows/ci.yml` 支持 `workflow_dispatch`。本交接提交推送后，应在当前分支手动触发
一次完整 CI；CI 必须运行到终态后再报告，不能把“已触发”写成“已通过”。

如果 CI 失败：

1. 先确认失败是否由当前交接或 `DV-B05` 变更引起；
2. 若只是分支历史债务或环境波动，如实记录，不修改 Runtime 或放宽门禁；
3. 只有当前提交直接造成的失败才在本分支做最小修复。
