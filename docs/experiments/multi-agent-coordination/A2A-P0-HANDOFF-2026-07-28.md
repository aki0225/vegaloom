# A2A P0 下一次工作交接

> 交接日期：2026-07-28
> 下一次继续日期：2026-07-29
> 工作分支：`experiment/ma2b-pilot-next`
> 预注册提交：`03929f2aed6ea79678b0f2aa5bc9e7a0670bc964`
> 当前状态：`design_frozen / p0_not_started / sdk_not_installed`

## 一、当前结论

当前只完成了 A2A 最小互操作探针的设计冻结。没有安装 A2A SDK，没有实现 P0，没有调用
Provider，也没有改变 MA-2B 正式 readiness。

A2A P0 只准备回答：

> 同一个确定性 fake Worker 通过直接 `WorkerAdapter` 和本地 A2A 协议路径执行时，能否保持
> 相同的允许写路径、最终文件内容、取消语义和固定 verifier 结果？

P0 即使通过，也只能说明协议路径有资格进入未来 P1，不能证明 A2A、Multi-Worker 或经济性
已经产生产品价值。

## 二、本次完成内容

提交 `03929f2` 完成：

1. 新增 `A2A-MINIMAL-INTEROP-PROBE-V1.md`，冻结唯一问题、P0/P1 边界、协议映射、资格条件和
   停止线；
2. 修正 `RESEARCH-AND-EXPERIMENT-PLAN.md` 中对已删除 `PlanContract.task_dag`、
   `HandoffPacket` 和新审计 ledger 的过时依赖；
3. P0 固定复用 `MA2B-F01`，不修改正式 task-pack、ground truth、hash 或 12-case readiness；
4. P1 仅作为未来选项，优先复用 `MA2B-C07`，仍需要 Owner 另行授权真实目标、网络和 Provider。

本次确认官方 Python SDK 候选版本为：

```text
a2a-sdk[http-server]==0.3.22
```

本机当前没有安装 `a2a-sdk` 或 `a2a`。

## 三、本次明确没有做

- 没有创建新分支；
- 没有创建 `.tmp/a2a-p0-venv`；
- 没有运行 `pip install`；
- 没有新增 A2A 代码、fixture 或测试；
- 没有修改通用 Runtime、Runner、Reviewer、CLI、CI 或产品依赖；
- 没有调用真实 Provider、Planner、Reviewer 或第二个 Worker；
- 没有创建 PR。

## 四、已完成验证

预注册提交前已完成：

```text
MA 定向测试：32 passed, 1 skipped
python -m compileall -q src scripts/check_repository_hygiene.py
ruff check src tests scripts/check_repository_hygiene.py
python scripts/check_repository_hygiene.py --base-ref origin/main
git diff --check
Markdown 尾随空白与末尾换行检查
```

这些结果只覆盖当前 MA 能力探针和文档修改，不是 P0 运行结果。

当前 CI 配置只在 `main`、`release/**` push 或目标为 `main` 的 PR 上运行。本实验分支直接 push
预计不会创建 CI run，因此远端没有 run 时必须报告为“CI 未运行”，不能报告为 CI 通过。

## 五、2026-07-29 严格执行顺序

### 1. 同步并确认分支

```powershell
git fetch origin --prune
git switch experiment/ma2b-pilot-next
git pull --ff-only origin experiment/ma2b-pilot-next
git status --short --branch
git log -3 --oneline --decorate
```

应从包含本交接文件的远端 HEAD 继续，不创建新分支。

### 2. 再次确认隔离依赖安装

安装属于包管理和联网操作，下一次执行前必须重新获得 Owner 明确确认。确认后只允许：

```powershell
python -m venv .tmp/a2a-p0-venv
.\.tmp\a2a-p0-venv\Scripts\python.exe -m pip install --no-cache-dir "a2a-sdk[http-server]==0.3.22"
```

安装范围只允许位于 `.tmp/a2a-p0-venv`。不得修改全局 Python、`pyproject.toml`、CI 或产品
依赖；不得安装 `all`、数据库、gRPC、telemetry、signing 等无关 extra。

### 3. 实现最小 P0

实现必须保持一次性 MA 实验边界：

1. 使用官方 SDK 的 JSON-RPC HTTP 路径和内存 ASGI transport，不启动公网服务；
2. 使用同一个确定性 fake Worker 分别执行直接路径和 A2A 路径；
3. 输入只使用现有 `MA2B-F01`，不得新增正式 Cxx case；
4. A2A 返回结果只允许包含仓库相对路径和文件内容；
5. Vega 本地在写入前校验路径，应用后运行同一个 F01 verifier；
6. 只实现正常 round-trip、越界 artifact 拒绝、取消后迟到 artifact 不落盘三个行为；
7. 不实现 streaming、push notification、动态发现、mailbox、认证系统或长期 task store。

优先写范围：

```text
src/vega/experimental/ma2b/a2a_probe.py
tests/test_ma2b_a2a_probe.py
```

预注册文件保持冻结，不因实现结果改写。需要记录结果时，只在
`RESEARCH-AND-EXPERIMENT-PLAN.md` 追加一个简短日期段落，不新建 ledger、receipt 或结构化
结果 JSON。

如果官方 SDK 无法在不修改通用 Runtime、产品依赖或 CI 的条件下完成 P0，应停止并报告，不得
自行扩展基础设施。

### 4. P0 结果解释

- 三个行为全部通过：只允许讨论是否进入 P1；
- 协议或 artifact 映射失败：停止，不新增通用层绕过；
- scope 或取消语义失败：拒绝当前方案；
- SDK、环境或版本问题导致未完成：如实记录为证据不足并停止；
- 不论结果如何，都不修改 MA-2B readiness，不自动进入 P1。

## 六、停止线

发生以下任一情形立即停止：

- 需要修改通用 Runtime、Runner、Reviewer、CLI、CI 或成功语义；
- 需要恢复 `PlanContract`、`HandoffPacket` 或新增证据 ledger/receipt；
- 需要放宽 task-pack、scope、verifier 或正式 12-case readiness；
- 需要调用真实 Provider、第二个 Worker、Reviewer、MA-3 或 multi-worker 产品化；
- 需要发送 API key、完整会话、隐藏推理、本机绝对路径或未裁剪仓库；
- 需要在没有真实跨运行时目标时继续扩建通用 A2A 框架。

## 七、当前权威文件

```text
docs/experiments/multi-agent-coordination/
  A2A-MINIMAL-INTEROP-PROBE-V1.md
  A2A-P0-HANDOFF-2026-07-28.md
  RESEARCH-AND-EXPERIMENT-PLAN.md
```

历史 MA-2B handoff 和 `eval/` 记录继续保留，不删除、不改写，但不能替代以上 A2A P0 当前
执行边界。
