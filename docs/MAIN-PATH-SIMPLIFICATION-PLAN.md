# 主路径精简计划

本轮只处理已批准的日常使用问题：环境准备反复交给 Worker、运行故障被当成业务 Replan、
继续入口与状态提示重复、最终报告缺少功能影响说明。机器事项为 `DAILY-01`；完成状态见
[CURRENT.md](CURRENT.md)。本文件描述范围，不代表已通过验收或已经发布。

## 保留、合并和删除

| 范围 | 实施决定 |
|---|---|
| 执行主线 | 保留同一 ChangeRun、单 Writer、Candidate 和 Core 门禁，不增加 Runtime |
| 环境准备 | 可选 `verification.prepare_commands` 编入批准合同，首次 Worker 前由控制器执行 |
| 普通实现问题 | 验证失败和 Reviewer finding 走原 Repair；改变目标或授权边界才修订合同 |
| 运行问题 | 明确 Reviewer timeout 沿原门禁链重试一次；可信证据重算沿原 Observation/Decision 路由 |
| 继续入口 | 保留 `change`；删除重复的公开 `run`、`retry` 和无调用 CLI 包装 |
| 状态提示 | `change`、`status`、`explain` 复用同一安全动作投影，删除 Agent `next_steps` |
| 最终报告 | 采用同次 Reviewer 可选的功能影响与代码位置，不增加总结模型 |
| 兼容 | 新 Run 使用执行协议 2；旧 Run 可查看、停止、可信交接，不能原地自动执行 |

备选是保留所有旧命令并增加修复提示；它不能减少重复职责，本轮不采用。内部仍被调用的
Core 验证恢复、Artifact 读取和 Task Card 恢复保留，不为压行数删除必要边界。

## 环境、恢复和授权

- 准备命令必须来自固定源版本的项目配置，并逐字进入批准合同。空配置保持原行为。
  非空准备命令需要人工批准，不能借 bounded 模式批准任意安装脚本。
- 同一批准摘要最多准备一次，开始执行前持久化预算。失败或进程终态未知时保留现场；
  不从错误信息猜安装命令，不自动重放未知写入。准备成功不代表验证通过。
- 准备期间允许响应停止，不能长时间持有状态锁。准备只能改变允许的环境内容；源码、
  未忽略文件、HEAD 或 Git 控制面变化必须停止。
  停止需绑定已启动的执行进程；命令间无活动进程时明确拒绝，不能报告已停止整个阶段。
  准备终态未知时没有自动恢复出口，活动绑定未解除前也不能生成可续跑的交接。
- Reviewer timeout 恢复继续遵守原 Candidate、策略、风险、进程与预算检查。
- Core 证据重算只接受当前合同、同一 Candidate、可解释 Workspace 和已结束的执行。
  原始失败记录保留，重算追加新的判断；没有可信测试或独立审查时不能产生成功。
  需要累计审查却缺少相应结果时继续交还人工。
- Worker 写入中断、未知外部副作用和授权变化不是自动恢复场景。

## 展示与交接

Reviewer 的功能影响属于审查意见。引用位置必须匹配冻结 Candidate；无效位置隐藏，
不编造行号、不改变门禁。缺少可选摘要时展示现有 Git 变更与验证事实。

旧 Run 的报告、Trace 和失败数据不删除。换机器仍通过任务分支、Task Card、合同和计划
创建新 Run，并重新核对现场；Provider 会话不作为可迁移的成功证据。

## 必要验收

1. 准备成功、准备失败、停止、未知终态与重启后禁止重复；批准页必须显示准备命令。
2. 实现失败仍可 Repair，证据不可信不能误走 Replan 或完成；重算不可重复、不可越界。
3. 同一 Run 的三种展示给出一致动作；旧命令退出，旧任务无法意外恢复 Writer。
4. 功能摘要无效路径或行号不进入可信代码导航，旧 Reviewer 输出仍可读取。
5. Codex、Claude 各一个受控日常任务，再用一个中等真实任务检查进度、交接、报告和干预。
   打回恢复由确定性测试验证；真实 Reviewer 自然打回时另行记录，不要求随机出现拒绝。
   Echo 旧任务只读保留，新任务只在隔离副本中执行，不修改或推送 Echo 主仓库。
6. 最小充分回归、静态与仓库门禁，之后由 PR CI 覆盖跨平台；不机械本地重跑全量。

使用一个实现分支和一个 PR；真实验收、独立审阅和 CI 通过后，按用户授权合并并处理重叠分支。
本轮不发布，也不增加 Provider、命令、状态、通用恢复系统。
若现有证据模型无法以小改动安全支持某项，先说明具体冲突，不用额外协议掩盖问题。

以上验收调整于 2026-09-10 获用户确认：不再以模型必须随机打回作为完成门槛，改为真实日用
与确定性恢复证据分开验收。`DAILY-01` 尚未进入主线，本次明确调整其验收条件；已有 started
事件及下方首轮失败、缺项记录保留，不将未发生的真实打回改写为已通过。

## 首轮候选记录（2026-09-10，保留当时状态）

本节记录首次提交候选时的状态，后续验收与合并安排以上方规则及文末追加记录为准。
实现位于 `feat/main-path-simplification`，包含尚未合并的 #116 修复。当时只提交候选 PR，
不合并或发布。`DAILY-01` 保持进行中，不能从下面的局部结果推断全部验收完成。

### 真实 Provider

| Provider | 版本 | Run | 结果 | Candidate 前缀 |
|---|---|---|---|---|
| Codex | 0.153.4 | `20260910-102001-f53786398856-agent` | `ready_to_commit`，exit 0 | `8ce049c2833c` |
| Claude Code | 2.1.246 | `20260910-102207-aecd65d48635-agent` | `ready_to_commit`，exit 0 | `ff46f70d225b` |

两次都是独立 Git 小仓库中的标签规范化任务，只允许修改 `labels.py`：去除两端空白、
过滤空项、按首次顺序去重、保持大小写敏感，格式化复用同一规则。固定五项 unittest
分别检查这些行为；控制器执行 `python -m unittest discover -s tests -q` 和
`git diff --check`，两条命令均成功。没有修改测试来取得通过。

按用户授权由主控批准显式合同；每次调用一个 Worker 和一个独立 Reviewer，没有调用
Planner。单进程 timeout 为 300 秒，Repair 预算 1、自动 Replan 0、Review 3、验证重试 1。
批准的 `python prepare_env.py` 只在 ignored `.prepared/` 写入标记；Trace 中准备完成
先于 Worker 启动。它证明执行顺序和 ignored 环境更新可用，**不证明第三方依赖安装可用**。

两个 Reviewer 都首轮 approve，并在同次审查返回功能说明。报告中代码引用已按各自 Candidate
核验。没有真实触发 Repair、Reviewer timeout 或 Core 证据重算，不能把正常运行冒充故障恢复。

运行采用冻结源码副本；源清单 SHA256 为
`2ba16e3dfd0ce436815b8cc4db5447b40749e58a50571822312234ebd0b08f06`。
运行后 `agent_verification_retry.py` 又补了重算保护，因此真实记录不是最终提交全部路径的实测。
原始日志、源码清单、复现脚本和报告保存在本机忽略目录 `.local-validation/d0910/`，未上传
Provider 会话或原始日志；换机后可以读本节结论，但不能凭它恢复这些本机 Provider Run。

### 定向验证

以下是分别结束且 exit 0 的检查，不相加为全量测试计数：

- 环境准备九场景、合同编译和 bounded 拒绝：11 passed；配置校验回归：14 passed。
- Core 证据重算完整文件：8 passed，包括 approve、打回 Repair、漂移拒绝、一次预算和来源约束。
- 报告合同及完整性：61 passed；Review Queue 预算分批节点：1 passed，未提高原 6700 预算。
- 状态解释、旧协议 Provider 控制及 Runtime 两节点：41 passed。
- 合同路由和旧协议执行拒绝：38 passed。
- 独立 clone 交接、恢复后再次交接、自动推进和 Fix Packet 接缝：5 passed。
- Ruff、编译、架构增长、仓库卫生与 Diff 检查通过；完整职责和跨平台留给 PR CI。

没有机械运行本地全量 pytest。故障恢复采用确定性测试，正常路径采用真实 Provider，二者分开。
架构门禁 C901 为 31 → 31；源码模块 237 → 241。本轮删除了重复入口和投影，但并非源码总量减少，
新模块用于环境准备、证据重算、配置校验抽离和报告引用核验。

### 仍待验收

1. 新实现尚缺一次真实 Reviewer 打回后沿原任务修复的记录；不故意制造模型错误凑通过数。
2. PR CI 通过后再决定合并；打包与 CI 结果以对应构建/检查为准，不能沿用 #116 的绿灯。
3. Echo 只读复核的是旧 Candidate `6a665ba8824ad1866f2a8d28c5b77a7035e9fc59`：
   前端 198、后端 367、五条验证和两次审查记录。旧真实打回与后续处理保留原样，
   不作为新版自动 Repair 的实测，也没有修改或推送 Echo。

下一会话先检查分支与 PR CI，再补第 1 项或明确调整验收范围。不要重启已完成的两个小任务，
也不要把准备终态未知、派生恢复摘要重算等保守限制扩成新恢复平台。

### PR 验证补充

候选已推送至 [PR #117](https://github.com/aki0225/vegaloom/pull/117)，保持草稿。
`875c051` 从干净 Git archive 构建 wheel/sdist，242 个源码与资源文件逐字节一致；独立 venv
安装 wheel、`pip check`、帮助、能力查询以及命令/协议检查通过。仍使用 0.5.1 包元数据，
这是开发候选而非发布。后续修复的包结果以新 CI 为准。

该提交首轮 CI 为 8 项通过、2 项失败，不能宣称全绿：Core Heavy 有一个旧 `run` 命令断言；
Supervisor 发现 Agent 文本渲染仍读取已删除字段、并发 fixture 未隔离环境准备、恢复诊断被
协议前置读取截断，以及同时损坏 Artifact/Trace 的测试先触发了另一种拒绝。修复沿原节点验证，
不通过恢复旧命令、放宽门禁或新增状态来隐藏问题。

后续修复的本地验证均 exit 0：渲染完整文件及 Core 接缝 12 passed，恢复及 Trace 邻近节点
13 passed，Skill 与并发锁原节点 2 passed。渲染改为复用现有 CLI 快照和安全动作；恢复保留
原诊断并在任何 Writer 操作前检查协议。最终跨平台结论请查看该 PR 最新 SHA 的 CI，
而不是首轮提交的检查结果。

### 独立审阅与第二次 CI

`89e47a0` 的十项 PR CI 均通过；随后独立审阅发现一处返回值错误：Core 证据重算已经把任务
完成，`change` 却仍报告需要人工处理。`89779d7` 修正 Driver 对 `completed` 的处理，继续使用
现有终态投影，没有添加成功路径。既有重算回归的两个参数分支均通过（2 passed，exit 0），
同时确认没有重新调用 Worker、Reviewer 或验证命令。该文件使用真实 Git 集成，两个节点共耗时
约 255 秒；它不是全量测试结果。

`89779d7` 的[十项 PR CI](https://github.com/aki0225/vegaloom/actions/runs/34433234174)全部通过，
包括职责分片、Windows、POSIX 与干净安装包检查。独立审阅已复核补丁，未发现其他新增阻断。
尚未以这份 CI 结果替代中等真实任务验收，也未发布新版本。

### 中等真实任务：生图迟到结果与会话切换

本次验收结果是**正确阻断，不是业务修复完成**。使用 Echo 的隔离副本，没有修改或推送其主仓库。

| 记录 | 值 |
|---|---|
| Vega 执行源码 | `89779d7071776bfe8ffd263f5fbdd3d527ce3334` 的独立冻结副本 |
| Echo 原始版本 | `65627d99c550e6615549baa9ce2d56d2ae16b21c` |
| 准备基线 | `1b31272bfb179078269bbabd691a13e70a7ad2a7`，只加入策略和 Trellis Task |
| Agent Run | `20260910-112559-f2304c07efe3-agent` |
| Core child | `20260910-113629-828520-bug-loop` |
| Candidate | `f72fa060432c27325383165ff9fc253a22afb891`，两文件，新增 151 行、删除 34 行 |
| 调用 | 一次真实 Planning、一次 Worker、一次隔离 Reviewer；Planning 与 Worker 复用会话 |
| 机器结果 | `needs_human`，exit 2；Core Finish 为 `needs_fix`，业务 Candidate 未被接受 |

任务要求 A 会话的生图迟到成功不能清空 B 的草稿、附件或消息，且成功后返回 A 仍能取得历史结果。
只允许修改 `ChatExperience.tsx` 和既有 `ChatPage.test.tsx`，不改后端、依赖、策略或流程文件。
异步状态路径登记为 `async-session` 风险，即使 Reviewer approve 也需要人工检查。

真实执行走完调查、合同编译、人工修订与批准、环境准备、Worker、Candidate、固定验证和独立审查：

- Planning 把“调查阶段只读”误写成整个任务不实施、不测试。主控在首次批准前通过 `revise`
  删除这一矛盾，并明确测试可使用临时 SQLite；原合同归档保留。没有扩大文件或验证范围。
- 两条准备命令由控制器先执行：离线锁定安装前端依赖、恢复当前 Trellis Task 指针；均 exit 0，
  总计约 108 秒。这次证明了实际依赖准备，不只是写入测试标记。
- 控制器验证：前端 **183 passed**、构建及 Diff 检查通过；后端因当前 Python 缺少 `pwdlib`
  在加载阶段退出 4。这是主控准备环境的遗漏，不能记为后端测试通过。
- Reviewer 指出两个时序问题并返回 `needs_human`。Risk Gate 同样要求人工，因此没有自动 Repair。
  Worker 所称“完成”和前端绿灯均未使任务进入可提交状态。

独立复核纠正了一次初审遗漏，使用原源码提取的 React hooks 做基线与 Candidate 的相同时序对照：

1. **生成结束前 A→B→A**：两版都可能把 B 的消息缓冲并入 A，属于既有问题，不能记为本次新增。
2. **B 历史尚未返回时，A 生图成功，再返回 A**：基线能看到 A 的生成结果；Candidate 只显示 A
   旧历史。新增提前返回没有失效驻留缓存，违反“成功后返回 A 能取得结果”的合同，足以拒绝该 Candidate。

这是局部源码与 React hooks 的确定性复现，不是完整浏览器或真实图片服务验证。探针 exit 0 表示
对照执行完成，不能解释成业务通过。首次“无阻断”的独立审阅结论已撤回，后续以本次复核为准。

为区分环境与业务问题，主控按 Echo 的 `uv.lock` 建立隔离后端环境后补验：首次 **365 passed、
2 failed**（exit 1），两项失败源于手工选择的临时目录不符合恢复演练的安全范围；改目录后的第一次
尝试又因父目录未创建得到 2 errors。准备好仓库 `.tmp` 子目录后，仅补跑这两个节点，**2 passed，
exit 0**。这些是人工补验证据，不回填或覆盖原 Core 的验证失败，也不包装成单次 367 项全绿。

本次并非零操作验收：主控准备隔离配置和 Task、纠正一次计划、批准合同、推进执行，并处理后端环境。
Worker 与 Reviewer 之间没有人工粘贴交接。没有真实发生自动 Repair、超时重试、压缩或跨机恢复，
这些能力仍使用前述确定性测试或既有记录，不能从本任务外推。

原始报告、失败日志和探针仅保存在忽略目录 `.local-validation/e0910/`。最终业务候选仍未合并；
下一步修 Echo 时先处理上述缓存退化，再核对往返切换的既有问题，不据此继续扩展 Vega 的恢复系统。

### 本次日用发现的 Vega 展示修复

Worker 正常写入时，`status` 显示 `acting`，解释区却因旧 Checkpoint 与工作区不同而要求人工处理。
修复只调整这一告警来源：当前 Writer 绑定正确、执行 lease 未过期且未请求停止时，继续显示执行中；
工作区与旧证据不同的事实仍保留。损坏证据、错误身份、过期 lease、停止未确认和采集失败均不能
取得这一展示豁免。损坏的 Decision 优先于活动状态或 Provider 交互提示。

独立审阅又发现损坏 lease 可能令新增展示分支抛异常，已改为保留原警告。最终定向验证分批结束：
直接场景 10 passed、既有负向与优先顺序 8 passed、最后修正的损坏记录和非法时间两节点
2 passed，均 exit 0；Ruff、C901 和 Diff 检查通过。只扩展两个既有测试文件，没有运行本地全量。
独立 Reviewer 已复核这四个文件，未发现剩余阻断。真实 Echo 使用的是前述冻结源码，该展示修复
由确定性测试验证，不伪称在旧 Run 的活动 Worker 上重新实测。

`DAILY-01` 的中等任务验收证明了真实独立审查可以阻止带退化的候选，未证明任务自动成功。
合并前仍以最后提交的 PR CI 为准；业务候选失败、人工补验和本轮未验证项不会因计划完成而改写。
